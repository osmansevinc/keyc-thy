package com.example.demo.service.service;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public interface IGenericService<T, D, ID> {
    final static Logger logger = LoggerFactory.getLogger(IGenericService.class);

}
